func main()
  var i,j : int
  var y: float 
  var p : pointer to int
  var pb : pointer to bool

  j = *p + (x>0);
  j = *i;
  y = 2 * (y+1);
  p = p + 1;
  y = *p + 1;
  if not *pb or y < 0 then
    write *p + *(p/2);
    i = **pb / 2;
  endif
endfunc
