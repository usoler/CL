func main()
  var i,j : int
  var y: float 
  var p : pointer to int
  if p == null then
    p = &i;
  endif
  j = *p + (x>0);
  i = &j;
  j = *i;
  y = null;
  y = 2 * (y+1);
  p = p + 1;
  if y == null then
     write "is null";
  endif
endfunc
